In this project,


upload and saveemployee method should be written in one method.

while adding the records in database add method should be written only once, and in that method all records including image should contain.




photo appears to be same for every name beacuse we are sharing all photos at sagar/sanket location, so just create random ID and store photo
in different locations 
